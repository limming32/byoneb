# 1. 폴더 이동 (앱 폴더로 이동)
cd 내_앱_폴더_경로

# 2. Git 초기화
git init

# 3. 모든 파일 스테이징
git add .

# 4. 첫 커밋
git commit -m "첫 업로드"

# 5. GitHub 주소 연결 (아까 복사한 주소 넣기)
git remote add origin https://github.com/yourname/react-survey-app.git

# 6. GitHub로 업로드
git push -u origin main
